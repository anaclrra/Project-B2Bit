# Desafio B2Bit - Front-end React TypeScript

### Instalando as dependências

instalando bibliotecas e dependências do projeto:

```bash
  npm install
```

## Rodando projeto

Após instalar as dependências necessárias, para rodar o projeto basta usar o comando:

```bash
  npm run dev
```